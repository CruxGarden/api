export const API_NAME = 'Crux Garden API';
export const MAX_ARTIFACT_SIZE = 250 * 1024 * 1024; // 250MB per file
export const MAX_PUBLISH_SIZE = 500 * 1024 * 1024; // 500MB total per publish
// A Crux Tool's built runtime is thousands of files (GDevelop: 2,647); one publish carries them all.
export const MAX_PUBLISH_FILES = 4000;
export const DELETE_CONFIRMATION_TEXT = 'DELETE MY ACCOUNT';
